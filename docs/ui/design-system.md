# Design system

Use existing UI primitives in `resources/js/Components/ui`. Prefer shadcn-style patterns already represented there. Prioritize consistency, accessibility, responsive layouts, loading states, empty states, validation, permission-aware actions, confirmation dialogs, professional spacing, and minimal visual noise.
