# Forms UI

Use Inertia `useForm`, `FormField`, labels, validation messages, and accessible inputs. Disable submit while processing. Show destructive confirmations. Server-side validation remains source of truth.
