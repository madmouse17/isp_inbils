# Components UI

Primitives live in `resources/js/Components/ui`. Composite patterns live in `resources/js/Components/composite`. Pages compose components; no repeated raw styled controls. Add missing primitive first, then use it.
