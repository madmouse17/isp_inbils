# Coding standards

PHP: PHP 8.2+, Laravel 12 conventions, explicit `$fillable`, no `guarded([])`, FormRequest for validation, Policy for authorization, Services for business rules.

TypeScript/React: TypeScript everywhere, avoid `any`, named function components, UI primitives under `resources/js/Components/ui`.
