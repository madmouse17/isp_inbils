# Git workflow

Branches: `main`, `develop`, `feature/*`, `bugfix/*`, `hotfix/*`, `release/*`. Use Conventional Commits. Never commit secrets. Never force-push shared branches. Review diff before merge.
