# Security rules

Never expose secrets. Never disable authorization. Validate all input at trust boundaries. Escape output where required. Protect destructive actions. Use least privilege. Review every permission change. Company-scoped data must remain scoped unless explicit approved cross-company operation exists.
