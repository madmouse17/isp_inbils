# ADR-001: Modular Laravel architecture

Status: Accepted

Use Laravel 12 with nwidart modules. Shared core stays in `app/`; domain logic stays in `Modules/{Name}`. Controllers stay thin, services own business rules, models represent persistence. Cross-module access uses services, actions, events, or explicit contracts. Direct module-to-module model imports are rejected.
